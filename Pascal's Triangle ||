class Solution {
    public List<Integer> getRow(int rowIndex) {
        List<Integer> row = new ArrayList<>();
        if(rowIndex<0){return row;}
            row.add(1);

        if(rowIndex==0){return row;}

        for(int i = 1; i<=rowIndex; ++i){
            for(int j = row.size()-1;j>0; --j){ // could use: row.length-1;
                row.set(j ,(row.get(j)+row.get(j-1)));
            }
            row.add(1);
        }
        return row;
    }
}
